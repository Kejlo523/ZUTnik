<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Keystore keys do not transfer. Settings can be moved with the in-app export. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
</full-backup-content>
